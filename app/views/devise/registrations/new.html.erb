
<%# あとでまとめる　_navbar %>
<nav>
  <div class="nav-wrapper brown lighten-3">
    <a href="#!" class="brand-logo">CampGohan</a>
    <ul id="nav-mobile" class="right hide-on-med-and-up">
      <li><a href="#">投稿する</a></li>
      <li><a href="#">カテゴリ</a></li>
      <li><a href="#">マイページ</a></li>
    </ul>
  </div>
</nav>

<%# あとでまとめる　_conform %>
<%# タスク　login sign_up　ページごとにviewの編集する必要有りのため、_navbarにまとめる %>
<div class="row">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field col s8 offset-s2">
      <%= f.label :メールアドレス %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
    </div>

    <div class="field col s8 offset-s2">
      <%= f.label :パスワード %>
      <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %> 文字以上)</em>
      <% end %><br />
      <%= f.password_field :password, autocomplete: "new-password" %>
    </div>

    <div class="field col s8 offset-s2">
      <%= f.label :パスワード %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
    </div>

    <div class="actions col s8 offset-s2">
      <%= f.submit "Sign up" %>
    </div>
  <% end %>

  <div class="col s8 offset-s2">
    <%= render "devise/shared/links" %>
  </div>
</div>